{
  "name": "cv-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "print-pdf": "astro build && node ./scripts/print-pdfs.mjs",
    "playwright:install": "playwright install chromium",
    "deploy": "astro build && node ./scripts/print-pdfs.mjs && gh-pages -d dist"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "astro": "^6.1.9",
    "gh-pages": "^6.3.0",
    "playwright": "^1.51.0",
    "serve": "^14.2.4",
    "tailwindcss": "^4.1.0"
  }
}
